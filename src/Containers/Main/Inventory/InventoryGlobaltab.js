import React from 'react'
import { connect } from 'react-redux'

export const InventoryGlobaltab = (props) => {
  return (
    <div>InventoryGlobaltab</div>
  )
}

const mapStateToProps = (state) => ({})

const mapDispatchToProps = {}

export default connect(mapStateToProps, mapDispatchToProps)(InventoryGlobaltab)